-need to write a login route: can't log in twice because it's actually a register components

--conditional formatting: only get edit and delete to appear when it's your own post

--to reset the token after: use the setToken function after the refresh
